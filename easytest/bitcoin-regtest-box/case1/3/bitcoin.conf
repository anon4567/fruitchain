# testnet-box functionality
regtest=1
#dnsseed=0
#upnp=0

# don't listen on a port, just connect to node 2
#listen=0
connect=localhost:16444

# listen on different ports than default testnet
port=16555
rpcport=16556

# always run a server, even with bitcoin-qt
server=1

# enable SSL for RPC server
#rpcssl=1

#rpcallowip=0.0.0.0/0

rpcuser=admin3
rpcpassword=123
